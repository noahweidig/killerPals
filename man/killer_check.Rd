% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessibility.R
\name{killer_check}
\alias{killer_check}
\title{Audit a palette's accessibility}
\usage{
killer_check(
  palette = "greatest_hits",
  n = NULL,
  type = c("qualitative", "sequential", "diverging"),
  direction = 1,
  colours = NULL
)
}
\arguments{
\item{palette}{Palette name, e.g. \code{"flash"} or \code{"opera_night"}. See
\code{\link[=killer_names]{killer_names()}}.}

\item{n}{Number of colours to return. Defaults to every colour the palette
defines. For \code{type = "qualitative"} this cannot exceed the number of
colours available, because inventing extra categorical colours would
silently break the palette's colourblind-safe separation.}

\item{type}{Palette type: \code{"qualitative"} for unordered categories,
\code{"sequential"} for ordered low-to-high values, \code{"diverging"} for values
spread either side of a meaningful midpoint.}

\item{direction}{\code{1} for the palette's natural order, \code{-1} to reverse it.}

\item{colours}{Optionally audit an arbitrary vector of colours instead of a
named palette. If supplied, \code{palette}, \code{n}, \code{type} and \code{direction} are
ignored.}
}
\value{
A list of class \code{killer_check} with components:
\describe{
\item{colours}{The colours audited.}
\item{min_distance}{Named numeric vector: the worst-case pairwise
CIEDE2000 separation under each vision type. For a qualitative palette
these should all comfortably exceed 10.}
\item{contrast_white,contrast_black}{Contrast ratio of each colour
against a white and a black background.}
\item{lightness}{CIELAB lightness of each colour.}
\item{monotone_lightness}{\code{TRUE} if lightness increases or decreases
steadily across the palette - the property that lets a sequential
palette survive being printed in greyscale.}
}
}
\description{
Reports the numbers behind the package's colourblind- and contrast-friendly
claims, so you can verify them rather than take them on trust.
}
\examples{
killer_check("greatest_hits")
killer_check("heavenly", n = 9, type = "sequential")

# Audit any colours at all, not just ours
killer_check(colours = c("red", "green", "blue"))
}
